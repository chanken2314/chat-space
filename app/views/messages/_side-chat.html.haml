.chat-side
  .chat-side-header
    %ul.chat-side-header__lists
      %h5.chat-side-header__name
        = current_user.name
      %li.chat-side-header__link
        = link_to new_group_path do
          = icon('fas','edit fa-white')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog fa-white')
        / ユーザー編集ページに遷移するためのパスを追加

  .chat-groups
    - current_user.groups.each do |group|
      .chat-group
        = link_to group_messages_path(group) , class: "info" do
          = group.name
          .chat-group__message
            =group.show_last_message